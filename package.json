{
  "name": "lehenga-backend",
  "version": "1.0.0",
  "description": "",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "sh -c 'npm run build && (tsc -p tsconfig.json --watch --preserveWatchOutput &) && node --watch dist/server.js'",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/express": "^5.0.6",
    "bcrypt": "^6.0.0",
    "express": "^5.2.1",
    "typescript": "^6.0.3"
  }
}
